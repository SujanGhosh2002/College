// Q12. WAP to display fibonacci series (i)using recursion, (ii) using iteration
#include<stdio.h>
void main()
{
    int num, first=0, second=1, i, next=0, sum=1;
    printf("Enter the number: ");
    scanf("%d",num);
    if(num==0)
    {
    	printf("Fibonacci series: %d",first);
	}
	else if(num==1)
	{
		printf("Fibonacci series: %d",first);
	}
	else
	{
		printf("Fibonacci series: %d",first);
		for(i = 2; i < num; i ++)
    	{
        	next=first+second;
        	
        	
    	}
	}
}